/**
 * Module index.js
 * Export app state modules
 *
 * @author  Marunin Alexey <amarunin@oneplus1.ru>
 * @since   1.0
 */

import navMenu from './nav-menu'
import locale from './locale'

const modules = {
  navMenu,
  locale
}

export default modules
